# Copie para .env e preencha. O .env esta no .gitignore.
# Credenciais do Postgres LOCAL de desenvolvimento. O banco publicado (S-26)
# tem credenciais proprias, que nunca passam por aqui.
POSTGRES_DB=solar
POSTGRES_USER=solar
POSTGRES_PASSWORD=
